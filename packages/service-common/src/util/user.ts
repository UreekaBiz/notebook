import { Identifier } from './type';

// ********************************************************************************
// === User =======================================================================
// NOTE: split from ../user simply to not have dependencies outside of this folder
export type UserIdentifier = Identifier;
