import { TaskListItemNodeType } from '@ureeka-notebook/web-service';

import { AbstractNodeModel } from 'notebookEditor/model/AbstractNodeModel';
import { NoStorage } from 'notebookEditor/model/type';

// == View ========================================================================
export class TaskListItemModel extends AbstractNodeModel<TaskListItemNodeType, NoStorage> {
  // no need to implement anything here
}
